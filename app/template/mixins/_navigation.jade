mixin navigation(className, items, activeItem)
  nav(class='#{className}__nav')
    ul(class='#{className}__list')
      each href, title in items
        li(class='#{className}__item' + (( title == activeItem ) ? ' #{className}__item--active' : '' ))
          a(class='#{className}__link' href="#{href}")= title
